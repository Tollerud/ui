import { Button } from '@/components/ui/Button'

export default function Home() {
  return (
    <main className="mx-auto flex max-w-md flex-col gap-4">
      <h1 className="text-xl font-semibold">Registry CLI smoke test</h1>
      <Button variant="primary">Copied via shadcn add</Button>
    </main>
  )
}
